{
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base /YYYY/slug/ --out ../../dist/YYYY/slug",
    "export": "slidev export --per-slide --output ../YYYY-MM-DD-slug.pdf"
  },
  "devDependencies": {
    "@slidev/cli": "catalog:",
    "@slidev/client": "catalog:",
    "@slidev/theme-default": "catalog:",
    "vue": "catalog:"
  }
}
